<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ucsd.cse110.ucsandeliever">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginActivity" />
        <activity android:name=".signupActivity" />
        <activity android:name=".requestActivity" />
        <activity android:name=".orderStatus" />
        <activity android:name=".orderHistroyActivity" />
        <activity android:name=".drawerActivity" />
        <activity android:name=".homeActivity" />
        <activity android:name=".ucsandelievery" />
        <activity android:name=".ViewRequestDetailActivity" />
        <activity android:name=".PlaceBidActivity" />
        <activity android:name=".balanceActivity" />

        <activity android:name=".userRunnerWaitingActivity"></activity>

        <activity
            android:name=".UserList"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustResize"></activity>
        <activity
            android:name=".Chat"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustResize"></activity>
        <activity android:name=".confirmPickingBidActivity"></activity>
    </application>

</manifest>